学习CS143的过程需要准备环境

线上资源提供虚拟机和在linux本机安装两种选项，显然都有点脱离时代了，遂尝试使用docker配置一个镜像环境进行编译原理的学习和cool编译器开发。

`docker run -it `


Docker 的核心其实是环境隔离。区别于虚拟机技术，docker的实质是在宿主机的linux内核下运行

通过Namespace机制，容器拥有独立的进程id计数和独立的网络设备以及文件挂载点。Cgroups进行资源的管理。而UnionFS提供一个灵活的文件系统，

容器的生命周期和主进程是相关的。容器内会有一个进程PID 1,这个进程结束则docker容器停止

