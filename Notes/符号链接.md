在配置cs143编译原理的docker镜像时，涉及到通过符号链接将系统深层目录和用户目录建立一个链接，即

`ln -s /usr/class/cs143 ~/cool`

就像是建立了一个快捷方式，以后
`cd ~/cool` 进入的目录就是 `/usr/class/cs143` 同款，更改也会同步变更，别名引用来的

删除快捷方式也很简单：
`rm -f ~/cool`
这样就可以重新建立链接了
